# P0 CRITICAL FIX — REAL-TIME MARKET DATA, NEWS & AI RELIABILITY

TradePilot is a real-time trading platform. Users have reported that live prices remain frozen, news is unreliable, and AI responses are inconsistent.

These are P0 issues.

Do NOT patch symptoms.

Find the ROOT CAUSE of every issue.

====================================================
ISSUE 1 — LIVE PRICE IS NOT UPDATING
====================================================

User feedback:
"The price is still the same."

Investigate the complete live market data pipeline.

Check:

- WebSocket connection
- Polling interval
- API refresh frequency
- Cache invalidation
- React state updates
- TanStack Query/SWR cache
- Stale data
- Market data provider
- Symbol switching
- Background tab behavior
- Mobile Chrome behavior
- Time synchronization

Verify:

- BTC price updates continuously.
- ETH updates continuously.
- Forex pairs update correctly.
- Price cards update without page refresh.
- Chart and price cards always match.

Never leave stale prices visible.

If live data is temporarily unavailable:

Show:

"Live market data is temporarily unavailable. Reconnecting..."

Never silently freeze the UI.

====================================================
ISSUE 2 — NEWS SYSTEM
====================================================

Audit the News module.

Requirements:

- Only display verified news from real providers.
- Never generate fake news using the LLM.
- Remove placeholder/mock articles.
- Remove duplicated articles.
- Sort by newest first.
- Show article timestamp.
- Show source name.
- Show loading state.
- Show retry button if provider fails.

If the provider is unavailable:

Display:

"Unable to load market news. Please try again shortly."

Never fabricate news.

====================================================
ISSUE 3 — AI RELIABILITY
====================================================

The AI must use:

1. Live telemetry
2. Current market data
3. Verified news
4. Historical data (if available)

Never claim:

"I don't have live market data"

if telemetry exists.

Never hallucinate prices.

Never hallucinate news.

If data cannot be verified, explicitly say so.

====================================================
ISSUE 4 — LATENCY
====================================================

Profile:

- Market data fetch
- News fetch
- AI request
- Context building
- Rendering
- Chart updates

Measure:

- API response time
- UI render time
- WebSocket latency
- AI response latency

Remove unnecessary:

- Duplicate requests
- Duplicate renders
- Blocking operations
- Sequential network calls that can run in parallel

====================================================
ISSUE 5 — MANUAL TESTING
====================================================

Test like a real trader.

Scenario 1:

Open TradePilot.

Watch BTC price for 5 minutes.

Verify price changes continuously.

Scenario 2:

Switch symbols rapidly.

Verify updates continue.

Scenario 3:

Disconnect internet.

Reconnect.

Verify automatic recovery.

Scenario 4:

Open multiple browser tabs.

Verify synchronization.

Scenario 5:

Test on Android Chrome.

Scenario 6:

Test on Desktop Chrome.

====================================================
FINAL REPORT
====================================================

Provide:

1. Root cause of frozen prices.
2. Root cause of news issues.
3. Root cause of AI inconsistencies.
4. Files changed.
5. Performance improvements.
6. Manual test results.
7. Production build status.
8. Remaining limitations.

Do not respond with "fixed."

Provide evidence (logs, screenshots, timings, and test results) that every issue has been reproduced, fixed, and verified.